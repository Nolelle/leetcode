class Solution:
    def nextGreaterElement(self, nums1: list[int], nums2: list[int]) -> list[int]:
        n1 = len(nums1)
        n2 = len(nums2)
        output = []

        for i in range(n1):
            index_2 = nums2.index(nums1[i])
            if nums2[index_2] > nums[]

        return output
